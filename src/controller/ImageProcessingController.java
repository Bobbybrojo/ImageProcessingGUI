package controller;

/**
 * Defines the operations that an image processing controller can perform.
 */
public interface ImageProcessingController {

  /**
   * Allows input to be passed to a model, be processed, and passed back through to a view.
   */
  void execute();



}
